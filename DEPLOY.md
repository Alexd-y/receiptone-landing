# 🚀 Инструкция по деплою ReceiptOne Landing

## Быстрый деплой на Vercel (рекомендуется)

### Вариант 1: Через веб-интерфейс Vercel (самый простой)

1. **Подготовьте репозиторий GitHub:**
   ```bash
   # Если Git еще не инициализирован
   git init
   git add .
   git commit -m "Initial commit: ReceiptOne Landing"
   
   # Создайте репозиторий на GitHub и выполните:
   git remote add origin https://github.com/Alexd-y/receiptone-landing.git
   git push -u origin main
   ```

2. **Деплой через Vercel:**
   - Откройте [vercel.com](https://vercel.com)
   - Войдите через GitHub
   - Нажмите "Add New Project"
   - Выберите репозиторий `receiptone-landing`
   - Vercel автоматически определит Next.js

3. **Настройте переменные окружения:**
   В настройках проекта → Environment Variables добавьте:
   
   **Обязательные:**
   - `NEXT_PUBLIC_CANONICAL_URL` = `https://your-domain.com`
   - `NEXT_PUBLIC_COMPANY_NAME` = `FLUTTER HORIZON`
   - `NEXT_PUBLIC_APP_NAME` = `ReceiptOne`
   - `LEAD_STORAGE` = `postgres` (или `firebase`)
   - `DATABASE_URL` = `postgresql://user:password@host:5432/dbname`
   - `NODE_ENV` = `production`

   **Опциональные:**
   - `NEXT_PUBLIC_ENABLE_ANALYTICS_CONSENT` = `true`
   - `NEXT_PUBLIC_GA_ID` = `G-XXXXXXXXXX` (если используете GA)
   - `NEXT_PUBLIC_GTM_ID` = `GTM-XXXXXXX` (если используете GTM)

4. **Нажмите "Deploy"**
   - Vercel автоматически соберет и задеплоит проект
   - После деплоя получите URL вида `your-project.vercel.app`

5. **Настройте кастомный домен (опционально):**
   - В настройках проекта → Domains
   - Добавьте свой домен и следуйте инструкциям по DNS

### Вариант 2: Через Vercel CLI

```bash
# Установите Vercel CLI глобально
npm i -g vercel

# Войдите в Vercel
vercel login

# Деплой
cd receiptone-landing
vercel

# Для production деплоя
vercel --prod
```

## Деплой на Netlify

1. **Подготовьте репозиторий GitHub** (как в варианте 1)

2. **Деплой через Netlify:**
   - Откройте [netlify.com](https://netlify.com)
   - "Add new site" → "Import from Git"
   - Подключите GitHub репозиторий

3. **Настройки сборки:**
   - Build command: `npm run build`
   - Publish directory: `.next` (или настройте для static export)

4. **Переменные окружения:**
   - Site settings → Environment variables
   - Добавьте все переменные из `.env.example`

5. **Деплой:**
   - Netlify автоматически задеплоит проект

## Настройка базы данных

### PostgreSQL (рекомендуется)

1. **Создайте базу данных:**
   - Используйте Vercel Postgres, Supabase, или другой провайдер
   - Получите connection string

2. **Выполните миграции:**
   ```bash
   # Локально
   npm run prisma:migrate
   
   # Или через Vercel CLI
   vercel env pull .env.local
   npx prisma migrate deploy
   ```

3. **Добавьте DATABASE_URL в переменные окружения Vercel**

### Firebase (альтернатива)

1. **Установите firebase-admin:**
   ```bash
   npm i firebase-admin
   ```

2. **Настройте Firebase:**
   - Создайте проект в Firebase Console
   - Создайте service account
   - Скачайте JSON ключ

3. **Добавьте переменные окружения:**
   - `FIREBASE_PROJECT_ID`
   - `FIREBASE_CLIENT_EMAIL`
   - `FIREBASE_PRIVATE_KEY` (полный ключ с `\n`)

## Проверка после деплоя

1. ✅ Откройте сайт по URL
2. ✅ Проверьте форму заявки (отправьте тестовую заявку)
3. ✅ Проверьте Privacy Policy и Terms страницы
4. ✅ Проверьте robots.txt и sitemap.xml
5. ✅ Проверьте мобильную версию
6. ✅ Проверьте Cookie banner (если включена аналитика)

## Troubleshooting

**Ошибка сборки:**
- Проверьте переменные окружения
- Убедитесь, что DATABASE_URL корректный
- Проверьте логи сборки в Vercel/Netlify

**Форма не отправляется:**
- Проверьте DATABASE_URL или Firebase credentials
- Проверьте логи функций в Vercel/Netlify
- Убедитесь, что LEAD_STORAGE установлен правильно

**CSP блокирует ресурсы:**
- Проверьте `next.config.js` - добавьте нужные домены в CSP

## Поддержка

При возникновении проблем:
1. Проверьте логи деплоя в Vercel/Netlify
2. Проверьте переменные окружения
3. Убедитесь, что база данных доступна

