# 🚀 Деплой ReceiptOne Landing - Прямо сейчас!

## Шаг 1: Авторизация в Vercel

Выполните в PowerShell:
```powershell
cd D:\Developer\ReceiptOne\receiptone-landing
vercel login
```

Откроется браузер - войдите через GitHub или email.

## Шаг 2: Первый деплой

После авторизации выполните:
```powershell
vercel
```

Vercel спросит несколько вопросов:
- **Set up and deploy "receiptone-landing"?** → Нажмите `Y` (Yes)
- **Which scope?** → Выберите вашу команду (Alexd's projects)
- **Link to existing project?** → Нажмите `N` (No - создать новый проект)
- **What's your project's name?** → Нажмите `Enter` (оставить receiptone-landing)
- **In which directory is your code located?** → Нажмите `Enter` (текущая директория `./`)

Vercel начнет сборку и деплой. Это займет 1-2 минуты.

## Шаг 3: Настройка переменных окружения

После успешного деплоя добавьте переменные окружения:

```powershell
# Обязательные переменные
vercel env add NEXT_PUBLIC_CANONICAL_URL production
# Введите: https://receipt-one.com (или ваш домен)

vercel env add NEXT_PUBLIC_COMPANY_NAME production
# Введите: FLUTTER HORIZON

vercel env add NEXT_PUBLIC_APP_NAME production
# Введите: ReceiptOne

vercel env add LEAD_STORAGE production
# Введите: postgres

vercel env add DATABASE_URL production
# Введите ваш PostgreSQL connection string (например: postgresql://user:pass@host:5432/dbname)

vercel env add NODE_ENV production
# Введите: production
```

**Важно:** После каждого `vercel env add` Vercel спросит значение - введите его и нажмите Enter.

## Шаг 4: Production деплой с переменными

После добавления всех переменных выполните:
```powershell
vercel --prod
```

Это задеплоит проект в production с правильными переменными окружения.

## Шаг 5: Настройка базы данных

После деплоя нужно настроить PostgreSQL:

1. **Создайте базу данных:**
   - Используйте Vercel Postgres (в настройках проекта Vercel)
   - Или Supabase: https://supabase.com
   - Или другой провайдер PostgreSQL

2. **Получите connection string** и обновите `DATABASE_URL`:
   ```powershell
   vercel env rm DATABASE_URL production
   vercel env add DATABASE_URL production
   # Введите новый connection string
   ```

3. **Выполните миграции:**
   ```powershell
   # Установите переменные локально
   vercel env pull .env.local
   
   # Выполните миграции
   npx prisma migrate deploy
   ```

## ✅ Проверка после деплоя

1. Откройте URL, который покажет Vercel (например: `receiptone-landing.vercel.app`)
2. Проверьте форму заявки - отправьте тестовую заявку
3. Проверьте страницы `/privacy` и `/terms`
4. Проверьте `/robots.txt` и `/sitemap.xml`

## 🔧 Полезные команды Vercel

```powershell
# Просмотр всех проектов
vercel list

# Просмотр деплоев проекта
vercel ls

# Просмотр логов
vercel logs

# Удаление проекта
vercel remove receiptone-landing
```

## 📝 Если что-то пошло не так

1. **Ошибка сборки:**
   - Проверьте логи: `vercel logs`
   - Убедитесь, что все переменные окружения установлены

2. **Форма не работает:**
   - Проверьте `DATABASE_URL` в переменных окружения
   - Убедитесь, что база данных доступна
   - Проверьте логи функций: `vercel logs`

3. **Нужна помощь:**
   - Документация Vercel: https://vercel.com/docs
   - Поддержка: https://vercel.com/support

---

**Готово! Ваш лендинг будет доступен по адресу, который покажет Vercel.**

