# 🚀 Деплой без Git (через Vercel CLI)

## Вариант 1: Установка Git (рекомендуется)

### Windows:
1. Скачайте Git: https://git-scm.com/download/win
2. Установите с настройками по умолчанию
3. Перезапустите терминал

После установки Git выполните:
```powershell
cd D:\Developer\ReceiptOne\receiptone-landing
git init
git add .
git commit -m "ReceiptOne Landing ready"
```

## Вариант 2: Деплой через Vercel CLI (без Git)

### Шаг 1: Установите Vercel CLI
```powershell
npm install -g vercel
```

### Шаг 2: Войдите в Vercel
```powershell
vercel login
```
Откроется браузер для авторизации.

### Шаг 3: Деплой проекта
```powershell
cd D:\Developer\ReceiptOne\receiptone-landing
vercel
```

Vercel CLI спросит:
- **Set up and deploy?** → `Y`
- **Which scope?** → Выберите вашу команду
- **Link to existing project?** → `N` (создать новый)
- **What's your project's name?** → `receiptone-landing`
- **In which directory is your code located?** → `./` (текущая директория)

### Шаг 4: Настройте переменные окружения

После первого деплоя добавьте переменные:
```powershell
vercel env add NEXT_PUBLIC_CANONICAL_URL
# Введите значение: https://receipt-one.com

vercel env add NEXT_PUBLIC_COMPANY_NAME
# Введите значение: FLUTTER HORIZON

vercel env add NEXT_PUBLIC_APP_NAME
# Введите значение: ReceiptOne

vercel env add LEAD_STORAGE
# Введите значение: postgres

vercel env add DATABASE_URL
# Введите ваш PostgreSQL connection string

vercel env add NODE_ENV
# Введите значение: production
```

### Шаг 5: Production деплой
```powershell
vercel --prod
```

## Вариант 3: Деплой через веб-интерфейс (без CLI)

1. **Создайте ZIP архив проекта:**
   - Выделите все файлы в папке `receiptone-landing` (кроме `node_modules`)
   - Создайте ZIP архив

2. **Используйте Vercel через GitHub:**
   - Создайте репозиторий на GitHub через веб-интерфейс
   - Загрузите файлы через веб-интерфейс GitHub
   - Импортируйте репозиторий в Vercel

## Рекомендация

**Лучший вариант:** Установите Git и используйте GitHub + Vercel интеграцию.
Это даст вам:
- ✅ Автоматические деплои при каждом push
- ✅ История изменений
- ✅ Preview деплои для pull requests
- ✅ Легкое управление версиями

## Быстрая установка Git

1. Скачайте: https://github.com/git-for-windows/git/releases/latest
2. Запустите установщик
3. Выберите "Git from the command line and also from 3rd-party software"
4. Остальное - по умолчанию
5. Перезапустите PowerShell

После установки выполните команды из начала этого файла.

